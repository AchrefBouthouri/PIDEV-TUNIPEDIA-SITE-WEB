{% extends 'users.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
	<div class="grix xs4 container">
		{# {% for message in app.flashes('message') %}
			<div class="col-xs4">
				<div class="p-3 my-2 rounded-2 success"></div>
			</div>
		{% endfor %} #}
		<div class="col-xs4">
			<h1>Profil de {{person.FullName}}</h1>
		</div>
		<div class="col-xs1">FullName :</div>
		<div class="col-xs3">{{ person.FullName}}</div>
		<div class="col-xs1">Email :</div>
		<div class="col-xs3">{{ person.Email}}</div>
        <div class="col-xs1">Gender :</div>
		<div class="col-xs3">{{ person.Gender}}</div>
        <div class="col-xs1">Nationalite :</div>
		<div class="col-xs3">{{ person.Nationalite}}</div>
        <div class="col-xs1">Balance :</div>
		<div class="col-xs3">{{ '0.0 DT'}}</div>
		<div class="col-xs4">
            <a href="{{ path('users_profil_modifier') }}" class="btn blue">Modifier le profil</a>
            <a href="{{ path('users_pass_modifier')}}" style="margin-top: 10px;" class="btn blue">Modifier le mot de passe</a>
            {# <a href="{{ path('users_data')}}" class="btn grey">Voir mes données personnelles (RGPD)</a> #}
        </div>

		<div class="col-xs4">
		</div>
        {# {% for annonce in app.user.annonces %}
            <div class="col-xs1">Titre :</div>
        {% endfor %} #}
        
	</div>
{% endblock %}
